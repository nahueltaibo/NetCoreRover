namespace Rover.Core.Hardware
{
    public enum RemoteControlKeys
    {
        DirectionAxis = 0,  // Axis that determines the direction changes of the rover
        SpeedAxis = 1,      // Axis that determines the speed of the rover
        StartButton = 11,   //START button in gamepad
    }
}
